<h1>Editing Post</h1>

<%= form_for :post, url: post_path(@post.id), method: :patch do |f| %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
<% end %>

<%= link_to 'Back', posts_path %>
